# Notebooks

Thư mục này để sinh viên tự bổ sung notebook nếu muốn thử nghiệm thêm ngoài script `run_lab3.py`.

Khuyến nghị:
- ưu tiên chạy bằng `run_lab3.py` để đảm bảo output đúng cấu trúc starter kit;
- chỉ dùng notebook cho việc khám phá thêm, trực quan hóa, hoặc thử cấu hình phụ.
